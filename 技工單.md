# 內部技工單 —— 上線教學（Firebase + GitHub Pages）

跟著以下步驟做，完成後你會得到一個自己的網址，例如：
`https://你的帳號.github.io/dental-lab-order/`

不同裝置打開這個網址，資料會**即時同步**，不需要透過 Claude。

全部步驟大約 20 分鐘，只需要一個 GitHub 帳號和一個 Google 帳號，不用寫程式碼、不用打指令。

---

## 步驟一：建立 Firebase 專案

1. 瀏覽器打開 https://console.firebase.google.com
2. 用你的 Google 帳號登入
3. 點「新增專案」
4. 輸入專案名稱，例如 `dental-lab-order`
5. 「這個專案要啟用 Google Analytics 嗎」→ 選**不啟用**即可（不需要）
6. 點「建立專案」，等待約 30 秒完成

---

## 步驟二：啟用 Firestore 資料庫

1. 進入專案後，左側選單找 **建構 → Firestore Database**
2. 點「建立資料庫」
3. 位置選擇 `asia-east1`（台灣附近，速度較快）
4. 模式選「**正式環境模式**」（不要選測試模式，測試模式 30 天後會自動鎖住）
5. 點「啟用」

---

## 步驟三：設定安全規則

1. 進入 Firestore 頁面後，點上方「規則」分頁
2. 把裡面的內容整個換成：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      allow read, write: if true;
    }
  }
}
```

3. 點「發布」

> ⚠️ 這個規則代表「只要知道網址的人都能讀寫資料」，目前**沒有帳號密碼保護**（登入功能先不加，之後有需要的話跟我說一聲，我可以幫你補上）。因為表單有患者姓名、病歷號碼，**請不要公開分享這個網址**，使用範圍盡量控制在診所內部人員。

---

## 步驟四：註冊網頁應用程式，取得設定金鑰

1. 回到專案總覽頁（左上角有齒輪圖示旁的「專案總覽」）
2. 找到「新增應用程式」，點選 **`</>`（網頁）** 圖示
3. 應用程式暱稱隨意填，例如 `lab-order-web`
4. 「同時為此應用程式設定 Firebase Hosting」→ **不用勾選**（我們用 GitHub Pages）
5. 點「註冊應用程式」
6. 畫面會顯示一段程式碼，裡面有 `const firebaseConfig = { ... }`，把這一整段**複製起來**（先不要關閉這個畫面）

---

## 步驟五：把設定金鑰貼到檔案裡

1. 打開我提供的 `index.html` 檔案（用記事本、VS Code 或任何文字編輯器都可以）
2. 找到接近檔案開頭的這一段：

```js
const firebaseConfig = {
  apiKey: "AIzaSyAnGrJRJdb72UXgieJrGVwD09UeMNbk7k8",
  authDomain: "digital-technician-order.firebaseapp.com",
  databaseURL: "https://digital-technician-order-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "digital-technician-order",
  storageBucket: "digital-technician-order.firebasestorage.app",
  messagingSenderId: "136203522876",
  appId: "1:136203522876:web:9bf8167d1bfb6362c9c439",
  measurementId: "G-FNQC08ZQY2"
};
```

3. 把整段換成你在步驟四複製的內容（值要完全對應：apiKey、authDomain、projectId…）
4. 存檔

> 💡 這個 `firebaseConfig` 之後會被放進**公開的** GitHub 倉庫，任何人都看得到。這沒關係——Firebase 的網頁應用程式金鑰本來就設計成可以公開，真正的保護是靠步驟三設定的 Firestore 規則，不是靠隱藏這段設定。

---

## 步驟六：建立 GitHub 倉庫並上傳檔案

1. 瀏覽器打開 https://github.com，如果沒有帳號先免費註冊一個
2. 登入後，點右上角「+」→「New repository」
3. Repository name 填一個名字，例如 `dental-lab-order`
4. **Visibility 一定要選「Public」**（GitHub 免費方案的 Pages 功能只支援 Public 倉庫，選 Private 之後沒辦法用 Pages 發布）
5. 其他選項不用動，直接點「Create repository」
6. 建立完成後，會看到一個空倉庫頁面，點畫面中間偏上的「uploading an existing file」（或「Add file」→「Upload files」）
7. 把改好的 `index.html` 拖曳到上傳區
8. 下方 commit 訊息隨意填（例如 `first upload`），點「Commit changes」

---

## 步驟七：啟用 GitHub Pages

1. 在倉庫頁面，點上方「Settings」
2. 左側選單找「Pages」
3. 「Build and deployment」→ Source 選「**Deploy from a branch**」
4. Branch 選「**main**」，資料夾選「**/ (root)**」，點「Save」
5. 等待約 1 分鐘，重新整理這個頁面，上方會出現一行綠色的網址，例如：
   `Your site is live at https://你的帳號.github.io/dental-lab-order/`
6. 這就是你之後要分享出去的網址

---

## 步驟八：測試

1. 用手機開啟剛剛拿到的網址，選「我是送單者」，填一張單並送出
2. 用電腦（或另一支手機）開同一個網址，選「我是技工」
3. 確認技工畫面**馬上**出現剛剛送出的單子（不需要重新整理）
4. 技工端記得點一下「🔔 開啟通知提醒」，讓瀏覽器通知生效

---

## 之後要修改表單內容怎麼辦？

跟我說要改什麼，我會產生新的 `index.html`（記得把你原本貼的 `firebaseConfig` 那段保留住，或跟我說你的設定值，我可以直接幫你保留在新版裡）。拿到新檔案後：

1. 回到你的 GitHub 倉庫頁面
2. 點進 `index.html`，點右上角鉛筆圖示（或垃圾桶刪除後重新上傳也可以）
3. 如果用編輯：把內容整個刪掉，貼上新版內容，下方點「Commit changes」
4. 如果用重新上傳：先刪除舊檔案，再用「Add file → Upload files」上傳新的 `index.html`，一樣要用 `index.html` 這個檔名，否則 Pages 網址會失效
5. 存檔後約 30 秒到 1 分鐘，GitHub Pages 會自動重新部署，重新整理網址就會是新版本

如果你的倉庫「Settings → Pages」下方看到「Actions」分頁，也可以看到每次部署的進度和歷史紀錄。

---

## 安全性提醒（正式使用前務必考慮）

- 目前沒有登入機制，任何拿到網址的人都能看到/修改資料
- GitHub 倉庫是 **Public**，代表 `index.html` 原始碼任何人都能看到（但如前面說明，這對 Firebase 網頁金鑰來說是正常且預期的做法）
- 患者姓名、病歷號碼屬於個人資料，建議：
  - 網址不要公開張貼、不要讓搜尋引擎收錄
  - 內部使用範圍盡量控制在診所人員
  - 若之後要正式上線給診所使用，建議加上簡單的登入密碼保護——需要的話再跟我說，我可以幫你加 Firebase Authentication
